@ResponseBody 를 사용하면 뷰 리졸버( viewResolver )를 사용하지 않음
대신에 HTTP의 BODY에 문자 내용을 직접 반환(HTML BODY TAG를 말하는 것이 아님)

@ResponseBody 를 사용하고, 객체를 반환하면 객체가 JSON으로 변환됨

Optional

@Test

@AfterEach

assertThat();

테스트는 순서가 정해져있지 않기에 각 메소드별 의존관계가 없어야함